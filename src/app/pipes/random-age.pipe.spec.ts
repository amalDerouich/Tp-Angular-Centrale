import { RandomAgePipe } from '@angular/cli/tpAngular/src/app/pipes/random-age.pipe';

describe('RandomAgePipe', () => {
  it('create an instance', () => {
    const pipe = new RandomAgePipe();
    expect(pipe).toBeTruthy();
  });
});
